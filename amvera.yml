meta:
  name: lensflow
  environment: node
  toolchain:
    name: node:20

build:
  commands:
    - npm install --production

run:
  command: node server.js
  persistence:
    - /app/uploads
    - /app/lensflow.db
