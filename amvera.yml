meta:
  environment: node
  toolchain:
    name: "20"
    version: latest
build:
  commands:
    - npm install --production
run:
  command: node server.js
  persistence:
    - /app/uploads
    - /app.lensflow.db
