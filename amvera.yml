meta:
  name: lensflow
  environment: node
  toolset: "20"

build:
  dependencies:
    - node
  commands:
    - npm install --production

run:
  command: node server.js
  persistence:
    - /app/uploads
    - /app/lensflow.db
