meta:
  environment: docker
  toolchain:
    name: docker

build:
  dockerfile: Dockerfile

run:
  ports:
    - 3000
  persistenceMount: /data
  persistence:
    - /app/uploads
    - /app/lensflow.db
