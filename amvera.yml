meta:
  environment: node
  toolchain:
    name: npm
    version: "20"
build:
  commands:
    - npm install --production
run:
  command: node server.js
  persistence:
    - /app/uploads
    - /app.lensflow.db
